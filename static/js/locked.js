document.addEventListener('DOMContentLoaded', function () { const e = document.getElementById('countdown-hours'), t = document.getElementById('countdown-minutes'), n = document.getElementById('countdown-seconds'), o = document.querySelector('.lockout-box'); if (!o) return; const i = parseInt(o.getAttribute('data-unlock-time')); if (isNaN(i) || i <= 0) { const o = Math.floor(Date.now() / 1000); return void startCountdown(o + 3600) } startCountdown(i); function startCountdown(o) { function i() { const i = Math.floor(Date.now() / 1000); let c = o - i; if (c <= 0) return void (window.location.href = '/'); const d = Math.floor(c / 3600); c %= 3600; const r = Math.floor(c / 60), s = c % 60; e.textContent = d.toString().padStart(2, '0'), t.textContent = r.toString().padStart(2, '0'), n.textContent = s.toString().padStart(2, '0') } i(), setInterval(i, 1e3) } });
